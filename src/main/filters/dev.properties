application.host = 0.0.0.0
application.port = 8030

database.url      = jdbc:postgresql://${project.artifactId}-db:5432/postgres
database.username = postgres
database.password = root

docker.image.base = docker.io/azul/zulu-openjdk-alpine:25.0.1-jre

logger.root.level = INFO
logger.app.level  = INFO

functional-tests.server='http://${project.artifactId}:${application.port}/api/v1'